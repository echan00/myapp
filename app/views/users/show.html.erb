
 <%if user_signed_in? %> 
	if user.has_data
		* show data
		* show share
	else 
		* ask to install extension
<% else %>
	* show data
	<% if user_logged_in? %>
		* show share
	<% else %>
		* show sign up
	<% end %>
<% end %>

<h3>User</h3>
<p>User: <%= @user.name %></p>
<p>Email: <%= @user.email if @user.email %></p>
<p>This is where we show what apps the user uses</p>

<ul>
	<li>App 1</li>
	<li>App 2</li>
	<li>App 3</li>
	<li>App 4</li>
	<li>App 5</li>
</ul>
<br/><br/>
<div style="background-color: yellow; padding:10px;">If user signed in make them share here: show share url and easy to share twitter/linkedin urls</div>